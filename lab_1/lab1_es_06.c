#include <stdio.h>
#include <float.h>

int main()
{
    //finding the biggest and smallest values for float
    printf("%g \n", FLT_MAX);
    printf("%g \n", FLT_MIN);
    //finding the biggest and smallest values for double
    printf("%g \n", DBL_MAX);
    printf("%g \n", DBL_MIN);

    return 0;
}