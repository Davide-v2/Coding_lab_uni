#include <stdio.h>
#include <limits.h>

int main()
{
    //finding the biggest and smallest values for char
    printf("%d \n", CHAR_MAX);
    printf("%d \n", CHAR_MIN);
    //finding the biggest and smallest values for int
    printf("%d \n", INT_MAX);
    printf("%d \n", INT_MIN);

    return 0;
}